# Deployment metadata; this value is not exposed to browser code.
# Default: jsonmanager.rareobjectlabs.app
POC_DOMAIN=jsonmanager.rareobjectlabs.app

# JSON Manager never loads a JSON file automatically. There is no
# configuration for a startup file: the active file is chosen with the
# Choose File button in the UI on every server start.